---
description: Break work into small verifiable tasks with acceptance criteria and dependency ordering
---

Read and follow `.cursor/skills/planning-and-task-breakdown/SKILL.md`.

Read the existing spec (`SPEC.md` or equivalent) and the relevant frontend codebase sections. Then:

1. Enter plan mode — read only, no code changes
2. Identify the dependency graph between UI/modules (keep `www` vs `app` separate)
3. Slice work vertically (one complete user path per task, not horizontal layers)
4. Write tasks with acceptance criteria and verification steps (tests and/or browser proof)
5. Add checkpoints between phases
6. Present the plan for human review

Save the plan to `tasks/plan.md` and task list to `tasks/todo.md`.

If `tasks/plan.md` or `tasks/todo.md` already exists with unchecked tasks for different work, stop and ask before writing — never silently overwrite an incomplete plan.

# @repo/ui

Turbo starter scaffold. **Not used by `apps/app` or `apps/www`.**

Do not import `@repo/ui` until this package is either:

1. Replaced with a real shared design system, or  
2. Deleted from the workspace (preferred short-term — see `docs/ENGINEERING_DEBT.md`).

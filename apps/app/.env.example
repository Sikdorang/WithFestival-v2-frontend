# apps/app — Vite SPA (never commit real values)
VITE_API_URL=https://api.example.com
VITE_SOCKET_URL=https://api.example.com
# Optional. Leave empty to disable Sentry in local/CI.
VITE_SENTRY_DSN=

# Build-time only (not VITE_*) — used by @sentry/vite-plugin
SENTRY_ORG=
SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=
